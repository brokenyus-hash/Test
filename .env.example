# Keys can also be pasted in Settings inside the app. Environment wins as a fallback.
ANTHROPIC_API_KEY=
XAI_API_KEY=
# Protect the app when it is reachable from the internet (browser prompts for user "tavern" + this password)
APP_PASSWORD=
PORT=3000
